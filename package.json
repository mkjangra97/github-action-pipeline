{
  "name": "vite-docker-sample",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4600",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4600"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
